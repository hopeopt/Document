#清除工作区
clc
#清除终端
clear
#将矩阵文件读取进来加载为矩阵
X = readmatrix("文件名");
#数据标准化(标准化后一组同属性的数据均值为0，标准差为1)，X是二维矩阵的话，按列进行标准化；X是向量的话，将该向量进行标准化。
x = zscore(X);
#累加函数(将b中的数据进行累加)。
a = cumsum(b);
#求方差
a = var(b);
#求协方差
a = cov(b);
#奇异值分解(V是特征向量矩阵，D是特征值矩阵，主对角线就是特征值);
[V,D] = eig(A);
